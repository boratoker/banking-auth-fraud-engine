@startuml Transaction_Signing_Sequence
title TokerBank Kriptografik İşlem İmzalama (ECDSA P-256)

skinparam maxMessageSize 150
skinparam backgroundColor #FFFFFF
skinparam sequence {
    ArrowColor #2C3E50
    LifeLineBorderColor #34495E
    LifeLineBackgroundColor #ECF0F1
    ParticipantBorderColor #27AE60
    ParticipantBackgroundColor #2ECC71
    ParticipantFontColor #FFFFFF
    ParticipantFontSize 14
    ParticipantFontName Arial
}

actor "Kullanıcı" as User
participant "Frontend/Mobil\n(WebCrypto API)" as Client
participant "API Gateway\n(:8080)" as Gateway
participant "Signing Service\n(:8083)" as SignSvc
database "PostgreSQL\n(UserPublicKey)" as DB

== Aşama 1: Cihaz Kaydı (Device Enrollment) ==

User -> Client: "Cihazımı Güvenilir Kaydet" Butonuna Tıklar
activate Client

Client -> Client: ECDSA P-256 KeyPair Üret\n(Public Key + Private Key)
Client -> Client: Private Key'i Güvenli Alana Kaydet (Local/Keystore)
Client -> Client: Public Key'i SPKI/PEM Formatına Çevir

Client -> Gateway: POST /api/v1/signing/enroll\n(userId, publicKey, deviceName)
activate Gateway
Gateway -> SignSvc: İsteği Yönlendir
activate SignSvc

SignSvc -> DB: save(UserPublicKey)\n(Açık anahtarı veritabanına kaydet)
SignSvc --> Gateway: 200 OK (Cihaz Kaydedildi)
deactivate SignSvc
Gateway --> Client: Başarılı Yanıt
deactivate Gateway

Client --> User: "Cihazınız Güvenle Kaydedildi"
deactivate Client

== Aşama 2: Kriptografik İşlem Doğrulama ==

User -> Client: Para Transferi Başlat
activate Client

Client -> Client: İşlem Detaylarını JSON Yap\n(Örn: Tutar, Alıcı IBAN)
Client -> Client: JSON'u SHA-256 ile Hashle
Client -> Client: Hash'i Private Key ile İmzala\n(Signature)

Client -> Gateway: POST /api/v1/signing/verify\n(userId, payloadJSON, signature)
activate Gateway
Gateway -> SignSvc: İsteği Yönlendir
activate SignSvc

SignSvc -> DB: findByUserId()
activate DB
DB --> SignSvc: Kullanıcının Kayıtlı Public Key'i
deactivate DB

SignSvc -> SignSvc: SHA256withECDSA Doğrulaması Başlat
SignSvc -> SignSvc: Public Key ile Signature'ı Çöz ve Payload ile Kıyasla

alt İmza Geçerli (Signature is Valid)
    SignSvc --> Gateway: 200 OK (İşlem Onaylandı)
    Gateway --> Client: Transfer Başarılı
else İmza Geçersiz / Manipülasyon Var
    SignSvc --> Gateway: 400 Bad Request (Cryptographic Error)
    Gateway --> Client: Güvenlik İhlali! İşlem Engellendi
end

deactivate SignSvc
deactivate Gateway
Client --> User: Sonucu Göster
deactivate Client
@enduml
